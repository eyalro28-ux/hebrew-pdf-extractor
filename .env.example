ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional shared secret protecting the paid /api/extract-vision endpoint.
# When set, requests must send a matching `x-api-token` header.
# Leave unset to keep the endpoint open (e.g. for local dev).
EXTRACT_TOKEN=
